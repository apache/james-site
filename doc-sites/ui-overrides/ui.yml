# Serve the favicons from the site root rather than from the UI bundle directory,
# so that browsers probing /favicon.ico and iOS probing /apple-touch-icon.png find them.
static_files:
  - favicon.ico
  - favicon.svg
  - apple-touch-icon.png
